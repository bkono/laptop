YOUCOMPLETEME_DIR = "$HOME/.dotfiles/vim/bundle/YouCompleteMe/"

if [[ -d "$YOUCOMPLETEME_DIR" ]]; then
  fancy_echo "Compiling YouCompleteMe ..."
    pushd $YOUCOMPLETEME_DIR
    sh install.sh
    popd
fi
